#include <stdio.h>
#include <stdlib.h>

#include "Elemento.h"

int abrir(Elemento* chave, Elemento* alvo)
{
	// verifica se é um objeto que está sendo aberto
	if(alvo->tipo) return 0;

	//verifica se o alvo requer uma chave

	//verifica se chave é a chave correta
}